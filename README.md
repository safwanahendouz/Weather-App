# Weather App (Python + Tkinter)

A simple desktop weather application built with Python and Tkinter that fetches real-time weather data using an API.  
Users can enter a city name to view temperature, weather conditions, humidity, and wind speed in a clean GUI.

## 🌦️ Overview
This is a simple desktop **Weather Application** built using Python and the Tkinter library. The app allows users to enter a city name and retrieve real-time weather information such as temperature, weather conditions, humidity, and wind speed.

This project is great for learning:
- GUI development with Tkinter
- API integration
- JSON data handling
- Event-driven programming

---

## 🧠 How It Works
1. The user enters the name of a city.
2. The app sends a request to a weather API.
3. The API returns weather data in JSON format.
4. The app parses the data and displays it in the GUI.

---

## 🛠️ Requirements
- Python 3.x
- Tkinter (usually included with Python)
- requests library

Install required dependency:
```bash
pip install requests